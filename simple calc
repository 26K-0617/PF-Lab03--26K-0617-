#include <stdio.h>
int main(){
    //simple arith calc
    // uses switch read perc and switch on vale   case for divthat checjsfor zero divisor befoediv and print error if zero    default to handleany operator that is ot one of four
	int num1,num2;
    char ch;
	printf("Enter first number: ");
	scanf("%d",&num1);
    printf("Enter operator (+ - * /");
    scanf("%c",&ch);
	printf("Enter second number: ");
	scanf("%d",&num2);
    switch(ch)
    {
        case '+':
                printf("Answer:%d ",num1+num2);
        break;
        case '-':
                printf("Answer:%d"num1-num2);
        break
        case '*':
                printf("Answer:%d ", num1*num2);
        break
        case '/':
            { if (b>0){
                printf("Answer: %d",num1/num2);}
               else
                printf("Error! Division by zero is not possible");
            }}
        break;
    default: printf("Enter mentioned operators only");
             } 
    return 0;
}
